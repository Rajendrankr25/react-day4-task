import { AppBar } from "@mui/material";

export function MenuBar() {
    return (
        <div>
            <AppBar className="menu-bar">
                <h3>Dash</h3>
            </AppBar>
        </div>
    )
}